import assert = require('assert');

describe('postorgcreate hook install package', () => {
  // throw new Error('');
  //assert.fail('Here');
  it('should pass', function () {
    assert.ok(true);
  });
});
